{
  "image": "mcr.microsoft.com/devcontainers/python:3.10",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y libgomp1 build-essential && pip install --upgrade pip && pip install -r requirements.txt"
}